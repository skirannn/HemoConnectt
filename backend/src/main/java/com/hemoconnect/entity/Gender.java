package com.hemoconnect.entity;

/**
 * Used on DonorProfile for basic physical info, the same as the
 * `physicalInfo.gender` field in the original DonorBlood model.
 */
public enum Gender {
    MALE,
    FEMALE,
    OTHER
}
